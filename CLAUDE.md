# CLAUDE.md — PathLift 開発エージェント向け指示書

このファイルは、本リポジトリでコードを読み書きするAIエージェント（Claude等）への指示書です。
設計の「中身」は書きません。設計の正本は `設計資料/` 配下のドキュメント群です。
このファイルは「その正本をどう扱うか」「環境固有の制約」「横断的な規約」だけを定めます。

---

## 0. 最優先ルール（これだけは必ず守ること）

1. **設計資料を正本とする。** `設計資料/` 直下のドキュメントに書かれた設計・方針・スキーマ・構成に従って実装する。
2. **設計から外れる実装をする前に、必ずどちらかを行う。**
   - (a) 該当する設計資料を更新し、変更後の設計に合わせて実装する、または
   - (b) 設計を変えずに逸脱する正当な理由がある場合、その理由を該当ドキュメントに追記してから実装する。
3. **「とりあえず動かすために設計と違うことをした」状態を、ドキュメント未反映のまま残さない。** コードと設計資料が食い違ったまま放置することを禁ずる。
4. 設計レベルで判断に迷う／影響範囲が広い変更は、実装を進める前に人間に確認を求める。勝手に構成を変えない。

---

## 1. 実装に入る前の手順

コードを書き始める前に、関連する設計資料を読むこと。最低限、以下を確認する。

- まず `設計資料/README.md` で資料全体の位置づけを把握する。
- システム全体の構造・構成は `設計資料/C4_model.md` を参照する。
- 実行時の振る舞いは `設計資料/C4_runtime_diagrams.md` を参照する。
- データ構造を触る場合は `設計資料/schema_design.md` を必ず確認する。

該当する資料が見つからない、または設計が曖昧で実装方針が決められない場合は、
推測で実装せず、人間に確認するか、設計資料に方針を追記する提案を先に行うこと。

---

## 2. ドキュメントの置き場ルール

### 設計資料ディレクトリの構成方針

- **`設計資料/` 直下** — 確定した設計資料のみを置く。C4モデル図・スキーマ・方針文書など、
  実装の拠り所となる正本がここに集まる。エージェントが参照すべきファイルはここだけ。
- **`設計資料/仕様検討/`（またはその他の子ディレクトリ）** — 検討中・未確定の試料を置く。
  叩き台・議論メモ・比較案など、まだ正本になっていない資料はここに置く。
  エージェントはここのファイルを「現時点の正本」として参照してはならない。
  確定したら `設計資料/` 直下に移動（または同内容のファイルを作成）する。

### その他の置き場

- 設計・方針・仕様の本文 → `設計資料/` 直下に Markdown で置く。
- 図表の出力物（svg / html 等の生成物）→ `設計資料/assets/` に置く。
  本文の設計判断を assets 配下だけに埋もれさせない。要点は必ず本文側に書く。
- 一時ファイル・検証用の使い捨て → `設計資料/assets/tmp/` に置く。tmp の中身は正本として扱わない。
- 開発手順・進捗・引き継ぎなど「作業の記録」→ `開発管理/` 配下に置く。設計資料には混ぜない。

迷ったら「これは設計の正本か、作業の記録か」で `設計資料/` と `開発管理/` を切り分ける。

---

## 3. 横断的な規約

- 日本語ドキュメント内で、アルファベットの前後に半角スペースを入れない（スペースなしで統一）。
- 既存ドキュメントの文体・トーンに合わせる。過剰な主張や断定を避け、根拠を添える。
- コミットや変更提案では「何を・なぜ変えたか」を明示する。特に設計資料を更新した場合は、
  どの設計判断をどう変えたかが後から追えるようにする。

---

## 4. このファイル自体について

- このファイルに書かれた前提が現実と食い違っていることに気づいたら、
  黙って従わず、人間に指摘すること。古い前提のまま実装が進むことが最も危険。
